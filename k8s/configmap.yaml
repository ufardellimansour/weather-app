apiVersion: v1
kind: ConfigMap
metadata:
  name: weather-app-config
  namespace: default
data:
  PORT: "5000"
  # Note: Remplacez 'demo' par votre vraie clé API OpenWeatherMap
  # Pour obtenir une clé gratuite: https://openweathermap.org/api
